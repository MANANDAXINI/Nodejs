const math = require("./Math");

console.log("Math Value is", math.add(2, 4)); // Use the `math` object to call functions
console.log("Subs Value is", math.subs(4, 3)); // Ensure this matches the export name
